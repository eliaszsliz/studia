// Generated by CoffeeScript 1.12.7
(function() {
  (function($) {
    var MD_MAX_WIDTH, SM_MAX_WIDTH, contentWrapper, navbarCloseBtn, navbarOpenBtn, refreshGrid, sidebarClose, sidebarDom, sidebarOpen;
    MD_MAX_WIDTH = 991;
    SM_MAX_WIDTH = 767;
    window.addEventListener("orientationchange", function() {
      return refreshGrid();
    }, false);
    window.addEventListener("resize", function() {
      return refreshGrid();
    }, false);
    refreshGrid = function() {
      var cachedWidth;
      cachedWidth = $(window).width();
      return $(window).resize(function() {
        var newWidth;
        newWidth = $(window).width();
        if (newWidth !== cachedWidth && newWidth < 767) {
          cachedWidth = newWidth;
          sidebarClose();
          return sidebarDom.clearQueue();
        }
      });
    };
    sidebarDom = $('#sidebar-wrapper');
    contentWrapper = $('#content-wrapper');
    navbarCloseBtn = $('#navbar-sidebar-close');
    navbarOpenBtn = $('#navbar-sidebar-open');
    sidebarClose = function() {
      sidebarDom.animate({
        marginLeft: '-83%'
      }, '100', function() {
        return sidebarDom.removeAttr('style');
      });
      sidebarDom.addClass('sidebar-closed');
      navbarCloseBtn.addClass('d-none');
      navbarOpenBtn.removeClass('d-none');
      return contentWrapper.off('click');
    };
    sidebarOpen = function() {
      sidebarDom.animate({
        marginLeft: '0px'
      }, '250', function() {
        return sidebarDom.removeAttr('style');
      });
      $("html, body").animate({
        scrollTop: 0
      }, 600);
      sidebarDom.removeClass('sidebar-closed');
      navbarOpenBtn.addClass('d-none');
      navbarCloseBtn.removeClass('d-none');
      return contentWrapper.click(function(e) {
        sidebarClose();
        return e.stopPropagation();
      });
    };
    $('#navbar-sidebar-close, #sidebar-overlay').on('click', function() {
      return sidebarClose();
    });
    $('#navbar-sidebar-open').on('click', function() {
      return sidebarOpen();
    });
    return $('.clickable-row').click(function() {
      return window.location = $(this).data('href');
    });
  })(jQuery);

}).call(this);

//# sourceMappingURL=main.js.map
